# Override Router

- Dify id: `override-router`
- Dify type: `question-classifier`
- Flowise mapping: Custom JS Function + If Else + Set Variable

## Instruction

你要根據 `routing_mode_override` 的值，輸出對應的 mode 類別。
只輸出一個類別代號，不要解釋。
