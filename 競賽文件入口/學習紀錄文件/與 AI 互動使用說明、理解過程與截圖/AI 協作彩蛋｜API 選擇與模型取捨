## 這一段在講什麼

除了工具本身的選擇之外，我在專題開發過程中，其實也經歷了一段很實際的 **API 與模型選擇歷程**。  

這一段不像前面的功能與介面那麼容易被看見，但它其實很直接地影響了我整體開發效率、測試方式，以及我最後決定要把哪些 AI 工具真正納入專案工作流裡。

## 最早的選擇：Google AI Studio 與 Gemini API

我一開始最常使用的，是從 **Google AI Studio** 取得的 **Gemini API Key**。

這個選擇在專案初期其實很自然，因為我本來就已經和 Gemini 進行了大量的概念發想與規劃，所以當我要正式開始把 AI 接進系統裡時，也很直覺地先沿用 Gemini 的 API。

不過，隨著專案逐漸進入大量測試與反覆修正的階段，我很快就碰到一個很現實的問題：

**它提供的可用次數其實太少了。**

我印象很深，有一次我直接請 AI 幫我進行比較大量的批量測試，結果幾乎一次就把額度用完。這件事讓我第一次真正感受到：在原型階段，模型能不能被穩定地大量呼叫，對開發節奏影響非常大。

## 接著的嘗試：Groq

後來我有一度轉向使用 **Groq** 這種中間層服務，希望能找到一個在成本與可用性之間更平衡的做法。

不過，在我一開始實際使用他們免費模型的時候，整體感覺並沒有特別符合我的需求。無論是回應品質、使用手感，還是和我當時希望建立的陪伴型系統語氣相比，都讓我覺得沒有那麼理想。

所以到後來，當我真的進入「準備認真投入成本購買 API Token」這一步時，我就沒有把它當成主要考慮對象。

## 最後的決定：OpenRouter

真正讓我下定決心的時間點，是我開始非常認真地把這個專案當作一個需要被完整做出來的作品，而不是只是做做看。

在那個階段，我最後選擇了 **OpenRouter**，並且實際購買了大約 **10 美元** 的 API 額度。

我之所以會選 OpenRouter，最主要的原因是：

- 可以更彈性地挑選模型
- 使用上比較符合我當時的需求
- 對我來說，比較像是一個能支撐持續開發的入口

而我當時最想用的模型，是像 **ChatGPT 4o mini** 這類被普遍認為更擅長理解自然語言、也更貼近人類表達方式的模型。對我正在做的這類專案來說，這一點很重要，因為我的系統並不是單純要回答問題，而是要處理陪伴、引導、情緒理解與病人表達整理。

所以最後，我真正穩定採用的 API Key，是 **OpenRouter 的 API Key**。

## 這一段帶給我的理解

這一段讓我很深地理解到，AI 模型的選擇其實不只是比能力，而是同時要看：

- 可用額度
- 成本
- 穩定性
- 回應風格
- 是否適合專題的使用情境

也就是說，一個模型再強，如果額度太快用完、成本不適合高頻測試，或者語氣不適合病人互動場景，那它在真實開發流程裡就未必是最好的選擇。

## 為什麼這段是「彩蛋」

我會把這一段叫做彩蛋，是因為它不是系統表面最顯眼的部分，但其實非常真實。

很多時候大家看到的只是：

- AI 可以幫忙開發
- AI 可以幫忙寫 code
- AI 可以幫忙做前端

但在真正長時間開發時，背後其實還要一直做這種很現實的選擇：

- 哪個模型能撐得住測試量
- 哪個平台比較適合這個專案
- 哪種回應品質比較貼近產品定位

這些選擇雖然不一定會直接出現在最終畫面裡，但它們其實深深影響了我整個專案是怎麼被做出來的。

## 這一段的定位

如果要幫這一段下一個簡單定位，我會說：

**這是我從「先用能用的 API」逐步走到「開始根據產品需求、開發節奏與模型特性做選擇」的一段過程。**