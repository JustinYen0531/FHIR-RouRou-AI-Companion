# AI 協作歷程（三）｜從 Dify / Flowise 試作，到以 Codex 與 Antigravity 交錯開發的系統實作階段

## 這一階段我在做什麼

如果說前面幾個階段比較偏向概念發想與介面原型，那這一階段就是我真正開始把專案往「可運作系統」推進的過程。

這一段其實最複雜，因為它不只是單一工具的使用，而是一整段持續切換工具、切換架構、反覆試作與調整的開發歷程。我自己後來重新回頭看 Git 大事記，才比較能清楚整理出這一段到底發生了什麼。

## 最早的做法：先用 Dify 與 Flowise 嘗試 workflow 式開發

在專案早期，我一開始是使用 **Dify** 這類 AI workflow 工具來做聊天流程的實作，後來也曾嘗試 **Flowise**。

當時的想法比較接近：希望先透過工作流自動化方式，把整個陪伴系統的聊天路由、模式分流與知識讀取流程快速搭起來。

其中一個重要嘗試，是我曾經想在每次聊天前透過類似 **RAG** 的方式，先讀取和憂鬱症評估有關的細項內容，讓系統在回覆時更能貼近量表與臨床觀察方向。

## 為什麼後來放棄單純使用 workflow 平台

雖然這類 workflow 工具在初期很有幫助，但後來我逐漸發現幾個問題：

- 大量卡頓
- 平台相容性限制
- 某些資料流與實際需求不夠吻合
- 越做越複雜時，反而不容易細緻控制

所以後來我做了一個很重要的決定：

**不再把核心能力綁死在單一 workflow 平台上，而是把更多控制權收回到程式端。**

這也是為什麼後來系統逐漸改為使用 **Node engine** 的方式重建。

## Codex 與 Antigravity：我真正最常交錯使用的兩個開發工具

到了這個階段，我實際最常交錯使用的工具，就是 **Codex** 和 **Antigravity**。

對我來說，它們不是互相取代，而是分工很清楚。

### Antigravity 的角色

Antigravity 比較像是我在處理：

- 前端畫面
- 介面細節
- 截圖理解
- 視覺與互動調整

時最常依賴的工具。

我常常把它當成一種很像「看得懂畫面的開發助手」，甚至某種程度上像是我在用一個有 AI 能力的 VS Code。

### Codex 的角色

Codex 則比較常用來處理：

- 技術問題
- Bug 修復
- 架構規劃
- 實作步驟拆解
- 程式邏輯調整

我自己很常把 Codex 給出的實作計畫，直接複製貼到我的專案規劃文件裡。

它有一個我很喜歡的地方，是它在執行前常常會先列出步驟、請我審核，這讓我可以在每次放手讓它做之前，先確認整體方向沒有偏掉。

## 為什麼我要兩個一起用

我不是因為單純喜歡比較工具，而是因為實際開發時，兩者真的各有優勢。

一方面，Antigravity 在：

- 語音輸入
- 較自然的人性化回應
- 前端畫面理解

這些地方很順。

另一方面，Codex 在：

- 技術推理
- bug 修復
- 規劃能力
- 可審核的執行步驟

這些地方很強。

另外還有一個很現實的原因是：

**我會交錯使用它們，也是在做高強度使用額度的調度。**

例如當 Antigravity 額度碰到上限時，我就會切去 Codex 繼續開發；反過來也是一樣。這樣做不是隨意切換，而是一種很實際的效率最大化策略。

## 我實際怎麼下 prompt

我自己在這一段最常用的 prompt 方式，其實不是什麼很工整的指令模板，而比較偏自然語言協作。

我常做的事情包括：

- 直接上傳電腦截圖
- 告訴 AI 目前畫面背景與我想改的方向
- 請它幫我找出前端哪裡有問題
- 請它自己去看我的網頁資料夾
- 給它功能需求，請它拆出下一步要怎麼改

也就是說，我不是每次都先寫得很標準化，而是更像在和一個真的協作對象一起工作。

## 從 Git 大事記看，這段真正做出了什麼

如果只用感覺描述這段歷程，會很容易變成只是在講工具。但從 Git 大事記來看，這一段其實做出的成果非常實際，而且密度很高。

包括：

- 建立與修正 AI workflow / chatflow 架構
- 完成多模式 routing、風險路徑與 follow-up state
- 加入 HAM-D state tracking 與 structured data
- 補上 clinician summary draft、conversation simulation 與相關說明文件
- 建立手機 App 介面原型與多項 UI/UX 細節
- 將 FHIR 與 TW Core 整合進系統
- 完成 `fhirBundleBuilder`、validation layer、delivery API MVP
- 補上 consent preview、FHIR draft、HAPI upload、Patient ID 顯示與交付歷史管理
- 建立測試紀錄、競賽 README、runbook 與 FHIR 說明材料

也就是說，這一段不只是我「用了很多 AI 工具」，而是我用這些工具，實際把專案從聊天流程雛形，推進到一個具有：

- 前端原型
- 對話邏輯
- 醫師摘要
- FHIR 交付
- 測試紀錄
- 展示材料

的完整作品。

## 最後一段：ChatGPT 與 Notion MCP

在整個產品逐漸成形之後，我最後還會使用 **ChatGPT** 來做比較偏整理與記錄的工作，像是：

- 幫我統整過程
- 潤飾文字
- 轉成評審比較容易理解的筆記
- 連上 Notion MCP，把內容整理進 Notion

所以如果前面的 AI 比較像是在幫我「做產品」，那這一步的 ChatGPT 比較像是在幫我「整理產品脈絡與敘事」。

## 我自己的收穫

這一段對我最大的收穫，不是單純學會了幾個工具，而是我開始真正理解：

**AI 協作不是只靠一個最強工具，而是要知道在不同階段，哪個工具最適合承接哪一種任務。**

有些工具適合概念整理，有些適合畫面生成，有些適合 bug 修復，有些適合最後整理成能給別人看的文件。

而我這整段開發歷程，其實就是一直在做這種工具之間的轉譯、銜接與選擇。

## 這一階段的定位

如果要幫這一段下一個簡單定位，我會說：

**這是我把專案從 workflow 試作推進到實際產品開發的階段，也是我開始真正學會如何讓多個 AI 工具在同一個專案裡協作分工的一段歷程。**
